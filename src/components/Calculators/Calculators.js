import React from "react";
import { Container } from "semantic-ui-react"

export const Calculators = (props) => {
    return (
        <Container>
            <p>TBC</p>
        </Container>
    )
};
